<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SPV_Project.Profile"
             Title="Profile">
    <Grid>
        <Grid.RowDefinitions>
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
    
        <VerticalStackLayout Grid.Column="0" Margin="15,0,15,0">
            <Label Text="Ime:"/>
            <Entry Text="{Binding Ime}" IsReadOnly="True"/>
            <Label Text="Priimek:"/>
            <Entry Text="{Binding Priimek}" IsReadOnly="True"/>
            <Label Text="Uporabniško ime:"/>
            <Entry Text="{Binding UporabniskoIme}" IsReadOnly="True"/>
            <Label Text="Email:"/>
            <Entry Text="{Binding Email}" IsReadOnly="True"/>
        </VerticalStackLayout>
    </Grid>
</ContentPage>